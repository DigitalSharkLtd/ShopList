{
  "name": "shoplist",
  "version": "1.0.0",
  "homepage": "https://ВАШ_ЛОГИН.github.io/shoplist",
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "scripts": {
    "predeploy": "npm install && npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}
