{
  "name": "shoplist",
  "version": "1.0.0",
  "private": true,
 "homepage": "https://digitalsharkltd.github.io/ShopList",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "predeploy": "CI=false npm install && npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "GENERATE_SOURCEMAP=false react-scripts build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
